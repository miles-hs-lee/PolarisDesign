{
  "name": "@polaris/plugin",
  "version": "0.8.0-rc.5",
  "private": true,
  "description": "Claude Code plugin source — distributes the polaris-design skill, slash commands, and PostToolUse lint hook",
  "scripts": {
    "test": "node test/hook-smoke.mjs"
  }
}
